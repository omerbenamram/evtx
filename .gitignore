target
.idea
**/*.rs.bk
.DS_Store
.vs*
.history
bazel-*
result*

repomix-output.txt

# Samples are being copied by build scripts before deploying
**/public/samples/

profile/*
binaries/*
benchmarks/*
.PRE_PATH

# Local-only fixtures (e.g. PE/CRIM blobs for WEVT_TEMPLATE work). Keep out of git.
samples_local/

# Local vendor checkouts (for format research / patching upstream).
external/
perf/

# Large local benchmark/sample corpora (do not commit)
benchmark_parsers/
samples/EVTX-ATTACK-SAMPLES/
